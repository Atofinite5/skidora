[package]
name = "skidora"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CLI for Skidora Helix memory"

[[bin]]
name = "skidora"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
skidora-core = { path = "../skidora-core" }
