# Intake (Blueprint P0 Blockers Only)

Restated Intent: <one sentence>

## P0 — Blockers (Required to proceed)
1. <question> — choices: <a / b> — default: none

If two valid designs exist, ask **exactly one** P0 (pick A or B) and **wait**. Do not implement both.

Reply with answers as `P0.1 ...` to unblock implementation.
*(Surgical tasks must never use this template; default to zero paperwork).*
